<div class="container">
<h1>Latest Blog Post</h1>
<% @post.each do |post| %>
	<div class="well">
		<h4><%= post.title %></h4>
		<p><%= post.body %></p>
		<%= link_to 'Show', post_path(post), :class => 'btn btn-primary' %>
	</div>
<% end %>
</div>